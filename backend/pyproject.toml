[project]
name = "carecall-backend"
version = "0.1.0"
description = "Safety-first backend foundation for CareCall."
requires-python = ">=3.9"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
